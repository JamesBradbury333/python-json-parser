[[source]]
url = "https://pypi.org/simple"
verify_ssl = true
name = "pypi"

[scripts]
format = "black ."
lint = "flake8 -vv"
lexer = "python src/lexer.py"
tests = "pytest tests -vv"

[packages]
pytest = "*"

[dev-packages]

[requires]
python_version = "3.10"
